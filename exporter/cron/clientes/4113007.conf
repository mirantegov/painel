# Jussara (PR) — configuração de extração (SEM segredos; versionado).
# Segredos (DATABASE_URL do ERP + chaves MinIO) em 4113007.secret.env (gitignored).
IBGE=4113007

# ENTIDADES: VERIFICAR no ERP do cliente (query do runbook docs/runbook-exportador.md §2).
# Default abaixo é um chute (prefeitura/câmara/RPPS); ajuste antes de produção.
ENTIDADES="1, 2, 3, 4"

# (ano_atual - ANOS_BACK)..ano_atual p/ siscop/aise. Ou fixe EXERCICIOS="...".
ANOS_BACK=2
# apice (licitações/contratos): LIC_START..ano_atual.
LIC_START=2000

MANIFESTS="siscop,aise,apice"
