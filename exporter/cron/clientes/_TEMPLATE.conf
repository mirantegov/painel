# TEMPLATE — copie para <ibge>.conf e ajuste. (SEM segredos; este arquivo é versionado.)
# Adicionar um cliente = criar <ibge>.conf + <ibge>.secret.env. Isso é "um por cliente".
IBGE=0000000

# Entidades do município no ERP. Descubra com a query do runbook (docs/runbook-exportador.md §2):
#   select e.entidade, e.nome, e.tipoentidade from siscop.entidade e ... where ...ibge = '<IBGE>';
ENTIDADES="1, 2, 3"

# (ano_atual - ANOS_BACK)..ano_atual p/ siscop/aise. Ou fixe EXERCICIOS="2024, 2025, 2026".
ANOS_BACK=2

# apice (licitações/contratos): LIC_START..ano_atual. Ou fixe EXERCICIOS_LIC="2000, ..., 2026".
LIC_START=2000

MANIFESTS="siscop,aise,apice"
