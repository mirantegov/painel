# Handoff — situação atual e tarefas pendentes

> Estado em 2026-06-09. Branch padrão: `main` (limpo, 0 PRs abertas).
> Docs relacionados: `docs/CONTINUIDADE-SESSAO.md`, `docs/plano-simam-clickhouse.md`,
> `docs/clickhouse-epico5-design.md`, `docs/plano-fase-backend-v2.md`.

## Visão geral

Duas frentes ativas:
1. **#33 (3.7 long-tail)** — migrar os módulos restantes do mock inline para o
   padrão snapshot (Postgres). Incremental.
2. **Épico 5 — Armazém canônico SIM-AM (ClickHouse)** — schema entregue; falta
   ligar o ETL real e o sync para o Postgres.

Release **v2.0.0** já publicada (Épico 3 fechado). 9 módulos de entrega leem do
Postgres via snapshot.

---

## Frente 1 — #33 long-tail (snapshot)

**Feito:** `processos` migrado (#99). Padrão de migração PROVADO e documentado em
`docs/CONTINUIDADE-SESSAO.md` (seção "Padrão para migrar um módulo").

**Faltam 10 módulos** (ainda com dados inline no componente):

| Módulo | Componente | ~linhas | Complexidade |
|--------|-----------|--------:|--------------|
| defesa-civil | `components/defesa-civil.tsx` | 1447 | simples (inline) |
| frotas | `components/frotas.tsx` | 1469 | simples (inline) |
| patrimonio | `components/patrimonio.tsx` | 1931 | simples (inline) |
| obras | `components/obras.tsx` | 1986 | simples (inline) |
| educacao | `components/educacao.tsx` | 2668 | simples (inline) |
| saude | `components/saude.tsx` | 3037 | simples (inline) |
| assistencia-social | `components/assistencia-social.tsx` | 4137 | simples (inline, GRANDE) |
| previdencia | `components/previdencia/` | — | COMPLEXA (sub-tabs, `lib/demo-previdencia.ts`) |
| saneamento | `components/saneamento/` | — | COMPLEXA (sub-tabs, `lib/demo-saneamento.ts`) |
| legislativo | `components/legislativo/` | — | COMPLEXA (sub-tabs, `lib/demo-legislativo.ts`) |

**Sugestão de ordem:** seguir do menor inline (defesa-civil, frotas) para o maior;
deixar os 3 complexos (previdencia/saneamento/legislativo — já têm `lib/demo-*`,
mas sub-tabs e submódulos) por último.

**Receita por módulo** (resumo — detalhe no CONTINUIDADE-SESSAO.md):
1. Extrair consts de dados → `lib/demo-<slug>.ts` (`export` + `<SLUG>_SNAPSHOT`
   só com dados serializáveis, sem funções/JSX).
2. Componente: `useSnapshot("<slug>", <SLUG>_SNAPSHOT)` no topo; helpers ficam.
3. `lib/data/modules.ts`: garantir `<slug> → mod_<slug>` (tabela já existe).
4. `scripts/seed-demo.ts`: import + `insert into mod_<slug>`.
5. Verificar: `npm run typecheck`/`lint` (0 warnings, limpar órfãos) →
   `db:reset`+`import-ibge`+`seed-demo` → `/api/data/<slug>?ano=2026` →
   browser (tab oculta por default; habilitar via `localStorage` `dash-disabled-modules=[]`).
6. Workflow: branch `govtech42/...` → PR → review subagent → merge squash.

---

## Frente 2 — Épico 5 SIM-AM ClickHouse

**ENTREGUE** (#100 infra/schema/seeds, #101 qualidade extração). Tudo em
`infra/clickhouse/`.

**Estado atual (validado, 0 falhas):**
- Container `simam_clickhouse` (ClickHouse 25.3). HTTP `:8123`, nativo `:9100`
  (9000 é do MinIO `mirante-minio`). Senha `default/simam`. Dados em volume
  `./data` (gitignored). Sobe com `cd infra/clickhouse && docker compose up -d`.
- DB `simam`: **224 tabelas canônicas** (layout SIM-AM, tipadas) + **115 de
  domínio** (106 populadas, ~2,5k linhas reais: Banco, FontePadraoSTN, etc.).
- DB `simam_raw`: **224 tabelas de landing** (tudo String) p/ ingestão MinIO.
- Geração determinística do PDF (848 págs): `tools/parse_layout.py` →
  `gen_ddl.py` → `apply_batches.py` (lotes de 10). Correções curadas em
  `tools/overrides.json`.

**Tarefas pendentes (em ordem sugerida):**

1. **ETL real `simam_raw → simam`** (núcleo). Hoje só há template para 1 tabela
   (`schema/etl/README-pipeline.sql`, Empenho). Gerar os `INSERT … SELECT` com
   cast de tipos para as 224 tabelas — automatizar via script a partir do
   `tables.json` (mesma fonte do DDL). Materializar como views ou jobs.
2. **Ingestão MinIO → `simam_raw`**. Ligar a saída do exportador Go (Épico 4,
   eloweb→Parquet→MinIO) às tabelas raw via função `s3()`. Depende do formato/
   layout de paths do exportador (`s3://mirante/<base>/<entidade>/<exercicio>/<Tabela>.parquet`).
   Pôr ClickHouse e MinIO na mesma rede docker (`infra_default`) p/ resolução de nome.
3. **Sync `simam → Postgres`** (marts que servem o painel). Design em
   `docs/clickhouse-epico5-design.md` (database `mun_<ibge>` espelhando o schema).
4. **Curar campos residuais** (~5, logados por `parse_layout.py` como tokens
   descartados): `subItem`/`subTitulo` truncados → adicionar em `overrides.json`.
   Validar campo-a-campo os módulos críticos (Contábil: Empenho/Liquidacao/
   Pagamento; Receita) contra o PDF.
5. **Refinar PKs/ORDER BY** por tabela conforme as REGRAS DE IMPORTAÇÃO do layout
   (hoje `ORDER BY (_id_entidade,_exercicio,_competencia,+até 3 campos obrig)`).
6. **Reconciliar tipos** entre tabelas (um mesmo campo pode ter tipo diferente
   conforme a origem no PDF) — dicionário global de campos, se necessário.

**Mapa de mapeamento de tipos** (referência): Data→Date32, Valor/Decimal→
Decimal(p,s), Caractere→String, Numérico→UInt32/64 (ou Decimal se PIC com V),
sem-formato→inferido por prefixo (dt/vl/id/tp/cd/...).

---

## Épicos correlatos (contexto)

- **Épico 4** — Exportador Go eloweb→Parquet→MinIO. Já avançado (commits #94–98:
  schema siscop, entidade/exercicio, manifest 53 tabelas, multi-ano). É a fonte
  da ingestão da Frente 2, item 2. Ver memória `elotech-exporter`.
- **Épico 6** — Scrapers Python TCE/PR + SICONFI → Contas Públicas.
- **Épico 7 (#59)** — Supabase self-hosted próprio (produção, sem CLI).

## Como rodar (resumo)

```bash
# App (mock→Postgres via Supabase CLI)
npm run db:start && npm run db:reset && npm run db:import-ibge && npm run db:seed-demo
npm run dev                      # localhost:3000  (login: cliente 4117107 / CPF 00000000000)

# ClickHouse SIM-AM
cd infra/clickhouse && docker compose up -d        # :8123 HTTP, :9100 nativo
# regenerar schema do PDF: ver infra/clickhouse/README.md
```

## Convenções (CLAUDE.md)

Planos/docs em `docs/`. Slack → `#analytics` (C0B18NPV886). Ícones
`@hugeicons/react`. `next.config.mjs` ignora erros de build, mas rodar
`npm run typecheck`. Workflow por tarefa: branch `govtech42/...` → typecheck/lint
→ verificar no browser → PR → review subagent `caveman:cavecrew-reviewer` →
merge `--squash --delete-branch`. Co-autoria nos commits/PRs.
